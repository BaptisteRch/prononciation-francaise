{
  "name": "tailwind-project",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./src/default.css -o ./src/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.0"
  }
}
